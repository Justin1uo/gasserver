# Android BLE 模拟服务端 App — 优化需求文档

## 1. 项目概述

开发一个 Android App，作为 **BLE GATT Server（外围设备）**，模拟气体检测设备，
以 **5Hz** 的频率向 BLE Client（中心设备）持续发送 JSON 格式的实时数据。

> **通信对端**：客户端（GATT Client / Central）由其他团队开发，是一个 Android App，
> 通过标准 BLE API 扫描、连接本服务端并订阅 notify 来接收数据。
> 本项目仅负责服务端 App 的开发。

## 2. 传输方案

- **协议**：BLE GATT（低功耗蓝牙）
- **角色**：App 作为 GATT Server（Peripheral），暴露 GATT Service 和 Characteristic
- **传输方式**：Client 订阅 Characteristic 的 `notify`，Server 每 200ms 更新一次值并推送
- **MTU**：协商到 512 bytes（确保单次能传完 ~300 bytes 的 JSON）
- **Service UUID**：自定义（如 `0000fff0-0000-1000-8000-00805f9b34fb`）
- **Characteristic UUID**：自定义（如 `0000fff1-0000-1000-8000-00805f9b34fb`）

## 3. JSON 数据格式

每次发送的完整 JSON：

```json
{
  "NAME": "realtime",
  "CH4_CONC": 3.315977,
  "CH4_2f": 44.445282,
  "CH4_K": 0.077631,
  "C2H6_CONC": 0.128634,
  "C2H6_2f": 15.514145,
  "C2H6_K": null,
  "temp": 34.820000,
  "tec1temp": 35.014999,
  "tec2temp": 35.018002,
  "press": 40.021000,
  "flow": 0.000000,
  "pump1": 3000.000000,
  "pump2": 2500.000000,
  "enumb": 100.00
}
```

### 3.1 动态模拟字段（5 个）

#### CH4_CONC（甲烷浓度）— double

| 时间段 | 值域 | 行为 |
|--------|------|------|
| 0 ~ 30s | 2.5 ~ 3.5 | 在 2.8 附近随机波动，偶尔触碰 3.5 但不超过 |
| 30s ~ 60s | 3.5 ~ 10.0 | 带随机波动的上升趋势，从 3.5 线性增长到 10 |
| 60s+ | 10.0 ~ 20.0 | 继续上升，速率放缓 |
| 达到封顶 20 后 | 19.0 ~ 21.0 | 在封顶值附近 ±5% 随机波动，保持不变 |

- **波动特性**：每个数据点相对前一个点有 ±3% 的随机噪声，模拟真实传感器
- **连续性**：相邻数据点之间的跳变不超过当前值的 5%，保证曲线平滑
- **封顶值**：20.0

#### C2H6_CONC（乙烷浓度）— double

| 时间段 | 值域 | 行为 |
|--------|------|------|
| 0 ~ 30s | 0.06 ~ 0.12 | 在 0.09 附近随机波动，不超过 0.12 |
| 30s ~ 60s | 0.12 ~ 0.20 | 带随机波动的上升趋势 |
| 60s+ | 0.20 ~ 2.0 | 继续上升，速率放缓 |
| 达到封顶 2.0 后 | 1.9 ~ 2.1 | 在封顶值附近 ±5% 随机波动 |

- **波动特性**：与 CH4 相同的平滑随机波动
- **封顶值**：2.0

#### temp（温度）— double

- 目标值：35.0
- 波动范围：34.5 ~ 35.5
- 行为：在目标值附近 ±0.5 随机缓慢波动，无趋势

#### press（压力）— double

- 目标值：38.0
- 波动范围：37.0 ~ 39.0
- 行为：在目标值附近 ±1.0 随机缓慢波动，无趋势

#### enumb（电量）— double

- 初始值：100.0
- 衰减速率：每 60 秒下降 1.0（即每秒下降 ~0.0167）
- 行为：单调递减，允许 ±0.1 的微小波动
- 最低值：0.0（不低于 0）

### 3.2 固定字段（保持默认值不变）

| 字段 | 类型 | 固定值 |
|------|------|--------|
| NAME | String | "realtime" |
| CH4_2f | double | 44.445282 |
| CH4_K | double | 0.077631 |
| C2H6_2f | double | 15.514145 |
| C2H6_K | null | null |
| tec1temp | double | 35.014999 |
| tec2temp | double | 35.018002 |
| flow | double | 0.000000 |
| pump1 | double | 3000.000000 |
| pump2 | double | 2500.000000 |

## 4. 发送频率

- **频率**：5Hz（每 200ms 发送一次）
- **方式**：使用 `BluetoothGattServer` 的 `notifyCharacteristicChanged()` 推送
- **时机**：仅在有 Client 订阅（Client 写入 CCCD）时才开始发送，断开后停止

## 5. UI 界面要求

### 5.1 主界面元素

1. **开始/停止模拟按钮**
   - 点击后开始 BLE 广播 + 数据模拟
   - 再次点击停止广播和模拟
   - 按钮状态文字切换：「开始模拟」↔「停止模拟」

2. **连接状态显示**
   - 显示当前 BLE 广播状态：未广播 / 广播中
   - 显示当前连接的 Client 数量
   - 显示连接的 Client 设备名/地址（如有）

3. **实时数据预览**
   - 以表格或卡片形式展示当前发送的 JSON 内容
   - 动态字段（CH4_CONC, C2H6_CONC, temp, press, enumb）实时更新高亮
   - 显示已发送数据条数和运行时长

### 5.2 界面风格

- Material Design 3 风格
- 状态卡片用颜色区分：绿色=已连接，黄色=广播中未连接，灰色=未启动
- 数据预览区使用等宽字体，JSON 格式化显示

## 6. 数据模拟算法要求

### 6.1 平滑随机波动

每个动态字段使用以下策略生成连续值：

```
newValue = currentValue + (random.nextGaussian() * volatility * currentValue)
```

其中 `volatility` 控制波动幅度（建议 0.01 ~ 0.03），保证：
- 相邻值变化平滑，无突变
- 长期趋势符合时间段要求
- 高斯分布的随机性更接近真实传感器

### 6.2 时间驱动的状态机

```
Phase 1 (0-30s):    CH4 在 2.8±0.3 波动，C2H6 在 0.09±0.015 波动
Phase 2 (30-60s):   CH4 线性上升 3.5→10，C2H6 线性上升 0.12→0.20
Phase 3 (60s+):     CH4 继续上升至 20 封顶，C2H6 继续上升至 2.0 封顶
Phase 4 (封顶后):   在封顶值附近波动
```

## 7. 技术约束

- **最低 API**：Android 8.0 (API 26)
- **权限**：BLUETOOTH_ADVERTISE, BLUETOOTH_CONNECT, BLUETOOTH_SCAN (Android 12+)
- **语言**：Kotlin
- **架构**：MVVM，数据模拟逻辑与 UI 分离
- **容错**：
  - BLE 不可用时显示提示
  - MTU 协商失败时降级到 23 bytes 分包发送
  - Client 断开后自动停止发送，回到广播等待状态

## 8. 验收标准

- [ ] App 启动后可被 BLE 扫描发现
- [ ] Client 连接后每 200ms 收到一条 JSON 数据
- [ ] JSON 包含全部 15 个字段，格式与定义一致
- [ ] CH4 从 ~2.8 平滑上升到 20 后波动
- [ ] C2H6 从 ~0.09 平滑上升到 2.0 后波动
- [ ] temp 在 35±0.5 范围波动
- [ ] press 在 38±1.0 范围波动
- [ ] enumb 从 100 每分钟下降 1
- [ ] UI 实时显示连接状态和数据预览
- [ ] 开始/停止按钮正常工作
